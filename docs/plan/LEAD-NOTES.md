# Lead notes — cross-task requests the briefs could not settle

Collected from the brief-writers on Sept 3. Items marked **folded into T-01** are already in the frozen contract; the rest are for the lead to handle at the moment named. Update this file as PRs land.

## Folded into T-01 (nothing to do at kickoff)
- `marks_log` column set; `direct_quotes` table (direct mode only).
- `RefusalPayload.mark_status?: 'marked' | 'logged, cooldown' | 'no identity'`.
- `GET /public/observations?place_id=` (optional, T-40); direct-mode `POST /tasks` 202 quote + `POST /tasks/:id/confirm` + `X-Buyer-Signature`/`X-Buyer-Timestamp`; generic error bodies (429/413/403/401/404).
- `POST /session` dev path: a `walletAuth` SIWE payload from a **seeded** worker is accepted without an idkit-session and bound to `nullifierOf(worker)` (the CLI worker and the e2e harness).
- `POST /tasks` success code is **201**; the terminal insert prints `201` and no URL (T-28, T-34, T-44 aligned).
- `tasks` table names `answer`, `note`, `seeded`, `dispute_reason`, `auto_dispute_reason`; `buyer_token_hash` is sha256 hex; `buyerToken.ts` (T-19) is imported by T-16; `lifecycle.settleIfEligible` (T-17) is consumed by T-19.
- `POST /idkit/request` returns `{rp_context: {...}}` (wrapped); `GET /tasks/:id` supports `ETag`/`If-None-Match` and the `poll_after_seconds` 0/1/3 rule; the 409/500/503 error family; `reset-demo` requires `{confirm:'reset-demo'}`.
- Public `price_usdc` = 3.00 (worker rate) with `fee_usdc` 0.45 alongside; `agent_pays_usdc` 3.45 only on buyer-authenticated responses.

- `/public/*` response shapes pinned; `GET /tasks/:id/spec` (claimant-only) added; `POST /tasks/:id/submit` `proofHash` optional for `call-confirm`/`compare-two`; `NEXT_PUBLIC_ADMIN_UI` added to `.env.example`.

- Custom error names on all four interfaces (+ `IAbuseMark.BadOutcome`); `contracts/src/interfaces/Outcomes.sol` constants library; the `AbuseMark.outcome` value/tag mapping; `activeClaimOf` cleared on release/resolve/expire (one task per worker until it settles); `expire` sets no cooldown; no relayer/treasury setter in v0; `deployments/*.json` keys; `BUYER_AGENT_ID`, `BASESCAN_API_KEY`, `FORCE_REDEPLOY` env; `foundry.toml` fs_permissions + etherscan block (T-00).

- Subgraph `Buyer.taskCount`/`countedExternal` + the `Feedback` slot id; `ABUSE_CLASSES` tuple export; root `package.json` scripts pre-declared in T-00 (`osm:extract`, `inserts`, `demo:*`, `abi:gen`, `e2e:anvil`).

## To settle in T-01a/T-01b while writing them (Day 1)
- **`FakeChain` (T-07) `calls[]` must record role + decoded args (bigint for uint256)** — T-20 and T-30 tests read them.
- **`FakeChain` (T-07) must offer a settable custom-error revert** (e.g. `failNextWith('MarkCooldown')`) — T-30 and T-12/T-13 tests rely on it. Put it in T-07's §2 if it is not there when you review.
- **`PaymentGateway` (T-15) result variant for direct funding** — `requirePayment` may return `{ kind: 'quote', ... }` in direct mode so T-16b needs no interface change later. Ask T-15's agent to include the variant (one line) even though `DirectFundingGateway` stays a stub.
- **T-16's route must leave explicit stub calls** to `markIfIdentified`, `logScreening`, `upsertPoster` (T-30 fills them) — check in T-16's review; otherwise T-30 is `BLOCKED` on Day 3.
- **`api-contract.ts` per-route `description` strings and the session cookie names** — T-35 (OpenAPI) needs them; add during T-01b (cheap) rather than later.
- **`scripts/package.json` `inserts` script entry** — T-29 (lead-run) adds `demo:run`/`demo:reset`; add `inserts` at the same time so T-44 is not blocked on a root file.
- **`docs/spikes/RESULTS.md` is shared by T-02, T-03 and T-04 on Day 1** — the only same-day shared file in the plan. Rule: T-02 creates the file with every section heading pre-written (`## S1 … ## Timing`) and merges first (~17:00 UTC); T-03 and T-04 each edit **only** their own `## S3` / `## S1`+`## S5` sections and rebase before opening the PR. Merge order T-02 → T-04 → T-03 (T-03 lands later because the x402 spike is the longer box). `path-ownership` treats the file as shared for these three tasks only.

## Operator-facing decisions surfaced by the briefs
- The pack's ≤300-char showcase short description is **342 chars with placeholders**; T-48 carries the 264-char variant from 11-launch for the form.
- `wait_seconds` is **50** everywhere (Vercel Hobby 60 s ceiling), not the pack's 60; `SKILL.md`, MCP contract and the video copy say 50.
- `npx @legwork/mcp` requires publishing the package to npm (operator, Day 3–4); until then the README's from-source install line is the working one.
- `LEGWORK_API_URL`, `LEGWORK_DASHBOARD_URL`, `LEGWORK_INSERT` are agent-side env names used by the local MCP binary — documented in `packages/mcp/README.md`; decide whether they also belong in `.env.example` (they are not server env).

## Known pack-vs-brief drift the briefs already resolve
- T-01/`RefusalPayload` marks on **gate** hits of the six classes too (the pack sentence "the classifier's refusals are the only ones that mark" predates the deterministic gate).
- The CLI worker is seeded and uses the relayed routes (pack: direct `claim()`).
- `call-confirm.phone` matches the resolved OSM `phone`/`contact:phone` tag only (pack: "or the buyer-supplied listing").

- **T-11 follow-up (S PR after T-20):** consume `contracts/test/fixtures/attestation.json`; the fixture signer is the test key `uint256(keccak256("legwork-test-verifier"))` so no literal key is committed — `vm.sign(that, digest)` must reproduce the fixture signature.
- **DEP check at T-00:** `msw` and `@worldcoin/idkit-core` must be in `apps/api`'s dependency list (T-20 tests), `@electric-sql/pglite` in every package with DB tests.
- **`@legwork/subgraph-client` (T-09) must export `query(document, variables)`** besides the typed helpers — T-26 is told to `BLOCKED:` rather than write a fetcher.
- **Mount points across lanes (announce when the owning PR lands):** T-25's task list renders `<UnverifiedBanner>` when there is no session (T-42 supplies it); T-33's proof header links `Report task → /report/<id>` (T-42); T-10's `TaskRow` root carries `data-testid="task-row"` for T-39.
- **T-39 crop band:** the 9:16 column at 1920×1080 is x 656.25–1263.75 (width 607.5). T-39 asserts the true band; keep it.

## Decisions the lane-C briefs closed (strike them in review if you disagree)
- `activeWorkers` returns `lastCompletedAt >= sinceTs` **or** `registeredAt >= sinceTs` with `completed: 0`. Strict "completed in the last 7 days" cannot produce "4 active · 1 verified · 3 seeded" while the demo phone has no completion yet; the extension is what makes T-46's `seeded` → `n=1 (real)` flip coherent.
- `TaskResolved(toBuyer == false)` also bumps `Worker.completed`/`lastCompletedAt`, keeping the subgraph equal to the onchain `completed(nullifier)`.
- An over-cap spec returns the keyword result **without** `CLASSIFIER_TIMEOUT_LABEL` — nothing timed out; the label stays reserved for the four failure triggers (timeout, throw, `refusal` stop, null parse).
- The OSM extract queries business tags only, so a residential id outside it refuses as `region not covered` rather than `automated reconnaissance`; widening to residential buildings would store every home in Lisbon and blow the 5 MB cap. Bounding boxes: Leiria `39.68,-8.90,39.82,-8.70`, Lisbon `38.68,-9.25,38.83,-9.08`.
- `packages/screening/README.md` is T-06's file; T-22 owns only its `## OSM data` section (the ODbL attribution T-37 needs). Announce it when T-06 merges.

## Wave 1 landed (Sept 5) — announcements and decisions

Merged: T-05 (#67), T-06 (#69), T-08 (#68), T-09 (#66), T-10 (#65), plus lead PRs #70 (catalog: jsdom, testing-library, matchstick-as, assemblyscript; chain + minikit wired into `apps/api`), #71 (`immutable: false` on the five mutable subgraph entities) and #72 (extensionless relative imports in `packages/shared` and `packages/chain`).

- **Repo convention: extensionless relative imports in every workspace package.** Turbopack cannot resolve `./x.js` over `x.ts` once a package enters a Next app through a client component; `tsc`, `tsx` and `vitest` hide it. `.js` suffixes are gone from shared, chain, screening and subgraph-client. Do not write them in new code.
- **`@legwork/screening`** is importable from the package root: `screen`, `ScreenDeps`/`ScreenResult`/`ScreenLogEntry`, `Classifier`/`KeywordFallbackClassifier`/`FakeClassifier`, `JsonPlaceIndex`/`PlaceIndex`/`Poi`, `KEYWORD_RULES`/`ABUSE_CLASS_SLUG`, `FIRST_NAMES`. `packages/screening/README.md` is T-06's; T-22 owns only its `## OSM data` section.
- **`@legwork/subgraph-client`** exports `createSubgraphClient({url, apiKey?, fetch?})` with the generic `query(document, variables)`, the five helpers, `reducePreflight`/`activeSince`, and the recorded fixtures under `packages/subgraph-client/fixtures/` (`preflight.json` reduces to 4 · 1 · 3, median 9 `seeded`).
- **Dashboard mount points (T-10):** `TaskRow` root `data-testid="task-row"`, `EscrowMeter` `data-testid="escrow-meter"` with `data-state`/`data-progress`, mark counter `data-testid="mark-counter"`, present stage `.stage`; every chip carries `data-floor="32"`, narrated rows `data-floor="24"`. `TaskRow` itself appends the `call-confirm` disclosure. `getDashboardData('live')` returns the empty shape until T-26.
- **Mini-app (T-05):** primitives in `apps/miniapp/components/ui/*` are frozen; `app/api/idkit/*` are TEMPORARY and T-24 deletes them; `lib/*`, `mocks/**` pass to T-24. `NEXT_PUBLIC_WORLD_CREDENTIAL_LEVEL` is already in `.env.example`.
- **Task API (T-08):** `POST /session` answers `{worker, nullifier, mode, token}` and `requireWorkerSession` accepts `Authorization: Bearer`; walletAuth mode needs no idkit cookie (contract and `docs/api.md` updated with this sync). `GET /healthz` body is the §2 shape. `getTxQueue()` throws under `setChainForTests` — routes go through `getChain()`. A nonce is a `sessions` row with `mode = 'nonce'` and sentinel identity columns, consumed by deletion: **interim** until the schema gains `kind`, `consumed_at`, nullable `worker`/`nullifier` and `idkit_sessions.action` (lead `interface-change` + `0001` migration, then an S task to switch `session.ts`). `apps/api/drizzle/` is T-08's migration folder; `.default(0n)` in the frozen schema is now `sql\`0\``, so drizzle-kit needs no preload.
- **Subgraph (T-09):** `abi:gen` now writes an interface ABI into `subgraph/abis/` while the implementation is missing; `packages/shared/src/abi/TaskEscrow.json` regenerated. `graph test` still runs only locally — adding it to the `subgraph-build` CI job is a lead follow-up.
- **Still open for the lead:** the sessions schema columns above; the `reference` `CompareItem` can never fit under the 300-char cap (T-01b); CI `graph test`.

## Wave 2 landed (Sept 5, evening) — announcements and decisions

Merged: T-17 PR1 (#74) and PR2 (#83), T-18 (#75), T-19 (#76, round 2), T-20 (#77, round 2), T-21 (#78), T-22 (#79), plus lead PRs #80 (the path-ownership matcher), #81 (`nullifiers.worker` nullable, migration `0001_bright_exodus.sql`), #82 (this sync) and the wiring follow-up. All six tasks are closed on the issue board.

- **Convention: `pnpm --filter <pkg> test -- -t <name>`.** pnpm swallows a bare `-t` and runs the whole suite, so a §9 that "proved" one test proved all of them. Every brief's §9 now carries the `--`.
- **Convention: copy the `owned-paths:` block verbatim from the front matter.** `scripts/ci/path-ownership.sh` (#80) expands `{a,b}`, escapes `[id]` itself, and accepts a body that already escaped them; it runs under macOS bash 3.2 too, so a reviewer checks a PR with `bash scripts/ci/path-ownership.sh "$(gh pr view N --json body --jq .body)" main` from a checkout of the branch.
- **`FakeChain` records `calls[]` (`{fn, role, args}`, bigint for uint256) and offers `failNextWith(name)`.** The recording Proxies in T-17's, T-19's and T-20's tests can go in their next PR. Roles are the package's own: `relayer`, `owner` (the `DEPLOYER_PRIVATE_KEY`; the briefs said `deployer`), `signer`, `sender` (direct mode).
- **There is no `TxQueue.send({role, …})`.** Routes call the adapter — `getChain().claimFor(…)`, `.registerFor(…)`, `.approve(…)`, `.pause()` — and the role is bound to the method; `getTxQueue()` throws under `setChainForTests`. The T-19/T-20 briefs are amended; write new briefs this way.
- **`@legwork/screening` root now exports** `AnthropicClassifier`, `createLiveClassifier` (the repo's only `ANTHROPIC_API_KEY` reader), `DEFAULT_CLASSIFIER_MODEL`, `OsmPlaceIndex`, `getPlaceIndex()`, `loadExtract`, `checkDemoPlace`, `buildExtract`, `serializeExtract`. `getPlaceIndex()` reads `fixtures/osm/leiria-lisbon.json.gz` (52 418 business POIs, 1.6 MB) once at boot; `pnpm osm:extract` regenerates it — network, operator only, never CI. `prompt.md` and the gzip are located through `import.meta.url`; whether Next's bundler keeps that working when `apps/api` boots them is T-16's to prove.
- **Task API mount points:** `services/lifecycle.ts` (`loadTask`, `mirrorFromChain`, `workerBrief`, `titleOf`, `claimDeadlines`, `assertClaimableBy`, `revertName`, `eligibleAction`, `settleIfEligible` — `distanceM` comes from `geo.ts`), `services/proofStore.ts` (`ProofStore`, `MemoryProofStore`, `setProofStoreForTests`, `rehash`, `rawKey`, `imageKey`), `services/signedUrl.ts` (`signProofUrl`, `verifyProofUrl`, `WORKER_URL_TTL_S`), `services/geo.ts` (`round100m`, `distanceM`); with #76: `services/statusBus.ts` (`parseWait`, `versionOf`, `eligibleAction`, `applyTransition`, `buildTaskView`, `deps.sleep`) and `services/buyerToken.ts` (`newBuyerToken`, `hashBuyerToken`, `verifyBuyerToken`, `requireBuyerToken`, imported by T-16). The worker list is `GET /tasks/list`; `app/tasks/route.ts` stays POST-only for T-16.
- **Contract settled in this sync (`docs/api.md` regenerated):** `GenericError` is one member per name carrying the fields the routes send (`InCooldown.cooldown_until`, `AlreadyClaimed.active_task_id`, `chain_revert.name`, `not_eligible.eligible_at`, `conflict.reason`, `attestation_rejected.name`); an oversized `POST /proofs` is **413 `payload_too_large {max_bytes}`** — T-18's code flipped here, the brief said 400; `GET /proofs/:hash?exp=&sig=` is listed; verify carries `exists`, `size_bytes`, `served_hash`; `/public/feed` and `/public/task/:id` return `PublicTaskView` (`price_usdc` 3.00, `links`, `coordinate_rounded` inside `proof`, no `url`, no note); `/public/refusals` is `{classes[6], recent, examples}` with no `reason`/`spec_hash` in `recent`; `/public/posters` gains `source`; `GET /config/world` is listed; `submit` gains `dispute_tx`; `rate_limited` carries `retry_after_s` (T-08's limiter renamed to match).
- **Decisions the reviews closed:** `compare-two` writes no `observations` row (the shared schema marks it `notApplicable`); a seeded observation carries confidence 0 (shared schema); `/me/earnings` sums `released` only (the T-01 row — a `resolved_to` column is a later schema call); `nameDistance` for `Farmacia` vs `Farmácia` is 0 (diacritics are stripped before the distance); the OSM re-run diff is empty only with `generated_at` masked; `task_id` is a decimal string on every surface (`TaskId`), and `/admin/resolve` accepts a number too; `admin_audit`'s `route`/`body`/`outcome` live inside `payload`.
- **Operator items:** `demo-data.json`'s `node/2734018563` is not in the extract — pick the demo shop's real id (`checkDemoPlace(getPlaceIndex(), id)` is the one-command check; `node/650194167`, Farmácia Lis, has a phone) and note Leiria's geohash5 is **`ez1dp`** — the `ez5ku` the briefs carried until the wave-3 sync decodes to 40.23, −6.48, near the Spanish border. Vercel's serverless request body cap is 4.5 MB, below the route's 8 MB; the mini-app compresses client-side. `getProofStore()` falls back to memory without `SUPABASE_URL` — set it before the first real proof.
- **T-17 PR2 landed:** `services/proofChecks.ts` (`checkReuse`, `checkGeofence`, rule ids `submit-reuse`/`submit-geofence`), `services/sweeper.ts` (`sweep({clock?})`, `sweepIfDue`, `resetSweepClockForTests`), `services/reconcile.ts` (`reconcileTask`, `reconcileOpen`, the `state_drift` log line), `lifecycle.settleIfEligible(taskId) → {action, tx} | null` and `lifecycle.eligibleAction(row, now: bigint)` — `statusBus.eligibleAction(row, nowS)` wraps it and `GET /tasks/:id` calls the real `settleIfEligible` (wired by the lead after #83). `POST /admin/sweep` opens to `X-Admin-Key` or `X-Sweep-Secret` (a cron door, constant-time), audits both, and answers `{ok, expired, auto_released}` as integer arrays. An auto-dispute writes `tasks.auto_dispute_reason`, a `screening_log` row with `class: null`, and the observation at 0.1.
- **Still open for the lead:** a `secretOrAdminKey` variant of `admin/_shared.ts`'s `audited()` so `/admin/sweep` stops carrying its own audit row; `GET /tasks/:id/spec` (claimant-only) is in the contract and no brief owns it; the sessions schema columns (`kind`, `consumed_at`, nullable identity, `idkit_sessions.action`); CI `graph test`; the T-11 follow-up that consumes `contracts/test/fixtures/attestation.json` once #77 merges; `lifecycle.settleIfEligible` arrives with T-17 PR2 and T-19 drops its stub then; `listPosters` (T-30) and `preflightWorkers` (T-27) replace T-19's stubs when they land.

## Wave 3 landed (Sept 6) — announcements and decisions

Merged: T-24 (#85, round 2), T-26 (#86, round 2), T-27 (#87), T-31 (#93), T-35 (#88), T-38 (#89), T-40 (#90, round 2), plus lead PRs #91 (`@legwork/subgraph-client` into the dashboard), #92 (`@legwork/mcp`, `@legwork/subgraph-client`, `mcp-handler` into the API) and this sync. All seven are closed on the issue board.

- **Convention: a public surface never carries a requester identity, and the dashboard does not ask the API for one.** T-26's request for `feed[].buyer_agent_id` / `refusals.recent[].agent_id` was declined; the agent card reads `Task.buyerAgentId` and the `Mark` entity from the subgraph. Write dashboard briefs that way.
- **Convention: an isomorphic poll cannot read a server-only env.** `WORLD_CREDENTIAL_LEVEL` flipped the credential chip from the first browser tick until T-26 carried the server-rendered value through `getLiveDashboardData({ level })`. Anything the server resolves from a non-`NEXT_PUBLIC_` var travels with `initial`.
- **Leiria's geohash5 is `ez1dp` everywhere now** — every brief's `ez5ku` was replaced in this sync; `apps/miniapp/lib/area.ts` and the dashboard constants already say `ez1dp`.
- **Dashboard live mode is wired end to end:** `loadDashboardData(mode, { taskId?, state? })` is the awaited loader (`getDashboardData` stays synchronous for the frozen callers and returns the empty live shape); `/` renders `LiveMissionControl` (a client wrapper over `useLiveDashboard`, because a server page cannot pass T-26's render prop across the boundary), `/present` and the OG image load the same data, `?task=<id>` pins the filmed task on both. T-43 mounts `LiveDashboard` inside `PresentCanvas`. Receipt: `/task/<id>?t=<buyer token>` reveals the signed thumbnail; the token is read on the server, forwarded once, never rendered. `/admin` is `NEXT_PUBLIC_ADMIN_UI=1` at build time and the key is pasted at runtime. Fixtures with `msw` handlers live under `apps/dashboard/lib/data/fixtures/live/`.
- **API guards are live:** `apps/api/proxy.ts` (Next 16's name for `middleware.ts`, which now only warns; `config` must be a literal there, pinned to `edge.ts` by `proxy.test.ts`) wraps T-38's `middleware` — CORS allowlist (`MINIAPP_URL`, `DASHBOARD_URL`; no `Origin` passes untouched), `/admin/*` 404 unless `ADMIN_API_KEY` is 32+ chars, body caps (8 MiB `/proofs`, 16 KiB JSON, 411 for an unmeasured JSON body), sliding-window limits with `Retry-After`. `browse` (`GET /tasks`, `GET /tasks/:id`, `GET /public/*`) is **600/IP/min**, not the brief's 120: the dashboard polls four `/public/*` routes every 3 s per open tab. `src/log.ts` now merges T-38's `REDACT_PATHS` and uses the five-header allowlist serializer. `readJsonWithCap` is exported and not yet wired into a route.
- **`GET /public/preflight` is real:** it calls `preflightWorkers` (`src/services/preflight.ts`, the same `computePreflight` the hosted and local MCP servers run); zeros and `median_source: 'n/a'` without `SUBGRAPH_QUERY_URL`.
- **Contract settled in this sync (`docs/api.md` and `docs/mcp-schema.md` regenerated):** `publicObservations` matches T-40's route (`delta {checked_places, wrong_listings, by_source, sentence}`, rows carry `worker_verified`, `disclosure`, `?include_seeded=1`, 400 on a bad `place_id`); `Preflight.score_floor` is `min(score)` over verified active workers (seeded fallback, then 0); `HireHumanInput` is one object schema (fields advertised in `tools/list`, `spec` as the four per-type shapes, `Envelope` run in a refinement) built on the new `EnvelopeCommon` export; `approve_task` / `dispute_task` may answer `BuyerTokenRequired` `{ error: 'buyer_token_required', task_id, dashboard_url }` — the tools still answer `isError` text; switching them is a small follow-up.
- **`/openapi.json` (T-35)** is generated from `api-contract.ts` with `/admin/*` filtered out and the two `POST /tasks` routes merged into one operation; `x402` is an `apiKey` header scheme named `PAYMENT-SIGNATURE`.
- **`/public/observations` (T-40)** counts completed tasks only (join on `tasks`; `released`, or `resolved` to the worker via the audit row) and reconciles the newest 200 completions on every read, so T-17's provisional submit-time row is rewritten under the completion-time rule. `seeded = workerSeeded || task.seeded`.
- **`docs/mcp.md` is hand-written (T-31) and `pnpm docs:gen` writes `docs/mcp-schema.md`** instead of overwriting it. `SKILL.md` is final apart from `<hours>` (T-41).
- **Brief amendments in this sync:** T-25 owns `apps/miniapp/tests/tasks/**`, T-33 owns `apps/miniapp/tests/proof/**`; T-43 uses `await loadDashboardData(...)`; §15 of T-24, T-26, T-27, T-31, T-35, T-38, T-40 and T-43 records what each review settled.
- **`GET /tasks/:id/spec` exists (lead, after T-33):** worker-session, the current claimant only (403 `not_claimant` otherwise), `{ task_type, spec }` with the buyer's claims (`claimed_open`, `claimed_hours`, `claimed_state`, `source`) stripped for the same reason `workerBrief` strips them. T-42 reads `a`/`b`/`criterion_id` from it; T-33's call-confirm picker can read `template_id` + `slots` from it as a follow-up.
- **Still open for the lead:** the `tasks.resolved_to` column (T-17's `/me/earnings` and T-40's `resolutionsIn` both want it); the sessions schema columns; CI `graph test`; `secretOrAdminKey` for `/admin/sweep`; moving the observations reconcile behind `/admin/sweep` if it shows in latency; switching `approve_task`/`dispute_task` to the structured `buyer_token_required`; wiring `readJsonWithCap`; the `<hours>` placeholder (T-41); T-03/T-04 (class L) and the device merge-to-test for T-24 are the operator's.

## Wave 4 landed (Sept 6, night) — announcements and decisions

Merged: T-25 (#95, round 2), T-33 (#99), T-39 (#96), T-43 (#98), plus lead PRs #97 (brief amendments), #100 (`GET /tasks/:id/spec`) and this sync. T-13 was refused by `scripts/claim.sh` because T-04 is open; T-15 waits on T-03; both spikes are the operator's and gate everything on the chain and payment paths (T-13 → T-14 → T-23/T-29/T-32/T-37; T-15 → T-16 → T-28/T-29/T-30/T-36).

- **Convention: the mini-app's worker list is `GET /tasks/list`.** `apps/api/app/tasks/route.ts` is POST-only (T-16's stub); a mini-app that polls `/tasks` never loads in production, and T-24's mock answering both paths hid it. `pollRendersTasks` now asserts zero hits on `/api/tasks`.
- **Convention: a brief's "until added" fallback is conditional.** When the field exists on `main` (`WorkerTaskRow.brief` did), render it; T-25 shows `brief.subject`, `template_question` and `criterion_id` under the type-derived question.
- **`GET /tasks/:id/spec` is real** (lead, #100): worker-session, the current claimant only, 403 `not_claimant`, buyer claims (`claimed_*`, `source`) stripped. T-42 reads it; T-33's call-confirm picker may switch to it as a follow-up.
- **Mini-app proof flow (T-33):** `lib/gps.ts` never rejects and never invents a coordinate — the downgrade sends `gps_unavailable=true` + `worker_confirmed_at_place=true` and **no** `lat`/`lon`; the photo is re-encoded on the phone (canvas JPEG, long edge ≤ 1600, q 0.85, EXIF gone); `proofHash` and `photo_hash` both go in the submit body and the API checks they agree; `PaidState` renders nothing without the thumbnail; `/earnings` is `GET /me/earnings` and nothing else. The multipart upload bypasses `apiFetch` (boundary). jsdom's `Blob` needs the harness polyfill for `FormData` uploads in tests.
- **The legibility gate (T-39) is wired as the `e2e-dashboard` CI job** (this sync): Chromium at 1920×1080, `DATA_MODE=demo`, the two 1280×720 frames and `floors.json` uploaded as `present-mode-frames`. It was red on `main` between #96 and #98 by design — the gate found four real overflows in T-10's `present.css`, T-43 fixed them. A floor or crop failure is a finding for the CSS's owner, never a reason to touch the gate.
- **Present mode (T-43):** `useServerNow` reads the API's `Date` header through `/api/healthz` (round trip halved, 60 s re-sync, `· local` suffix as a sibling of the clock when it fails); the escrow meter transitions once per payout (`data-transitions`, `data-transition-ms="700"`, `is-transitioning`, `--meter-ms` → `0ms` under reduced motion, numerals plain text); `?hide=agent,supply,screening,row2,row3` cuts cards without moving the meter (fixed grid rows, columns placed by name); `data-column="left|centre|right"`, `data-row="1|2|3"` wrappers; one screening line in present mode; `?crop=1` draws the 9:16 guide. `/present?task=<id>` pins the filmed task through T-26's poll.
- **T-42 landed (#102) and is mounted (lead follow-up):** `/tasks` no longer redirects a visitor without a session — it renders T-42's `UnverifiedBanner` over the open rows of `GET /public/feed` (`app/tasks/UnverifiedTasks.tsx`), and the proof header links `Report task` → `/report/<id>`. `/compare/<id>` reads the claimant-only spec route; `/report/<id>` releases the claim before it reports, never in parallel.
- **Brief amendments in this sync:** T-42 owns `apps/miniapp/tests/optional/**` (the brief said `test/optional*.test.tsx` — wrong directory); T-33 §8's `one_paid` parenthetical says `score: 5` like the fixture; T-43 §15 records its rulings.
- **Usage limit:** every `claude -p` session shares the account's five-hour window; T-25's round 2 died on it at 16 turns and resumed cleanly after the reset with `--resume`. Three concurrent Opus sessions can exhaust an afternoon's window.
- **Still open for the lead:** the `tasks.resolved_to` column; the sessions schema columns; CI `graph test`; `secretOrAdminKey` for `/admin/sweep`; the observations reconcile behind `/admin/sweep` if latency shows; `approve_task`/`dispute_task` structured `buyer_token_required`; `readJsonWithCap` wiring; T-33's proof screen treating `resolved` + `tx.release` as released; the T-33 picker on `/tasks/:id/spec`; `<hours>` (T-41). Operator: T-03, T-04, the device tests for T-24/T-25/T-33, the rehearsal release for T-43.

## S3 landed (Sept 6, night)

T-03 (#105) — **PASS, `PAYMENT_MODE=x402`**, 11 minutes, three live round-trips on Base Sepolia with the reference facilitator; each moved 3,450,000 units exactly once and the replay of a captured `PAYMENT-SIGNATURE` settled nothing. `docs/spikes/RESULTS.md#s3` is what T-15 copies from. Findings the lead is holding T-15/T-16/T-28 to: the seller sets the EIP-712 domain via `getDefaultAsset`; the buyer passes `spendControls: { maxAmountPerPayment: "$5" }`; requirements travel in the `PAYMENT-REQUIRED` header and the JSON body is for humans; the nonce row is reserved **before** settle (T-16 §2 step 3 already says so); a failed settle is retryable and logs `float_absorbed=true`; poll before asserting a balance after settle. Root `package.json` now declares `@x402/*` and `viem` so `scripts/spikes/**` run from the root (#104). T-15 dispatched on the S3 section.

## Wave 5 in flight (Sept 7, small hours) — announcements and decisions

Merged: T-15 (#107), T-16 (#108), plus lead PRs #104 (root `@x402/*` + `viem` so `scripts/spikes/**` run from the root), #106 (`payment: x402` locked), #109 (`apps/api` declares `@legwork/payments`, `@legwork/screening`, `@x402/core`; `packages/payments` drops its `.js` import suffixes; the 501-stub test removed), #110 (`packages/mcp` devDepends on `@legwork/payments`; `.env.example` names `LEGWORK_API_URL`, `LEGWORK_DASHBOARD_URL`, `LEGWORK_INSERT`) and this sync. T-28 dispatched on the T-16 `main`.

- **`POST /tasks` is real (T-16):** `hire()` in the frozen order — price read → budget echo + `requirePayment` → `reserve(nonce)` → screen → caps → agent id → `TaskEscrow.post` → row + reservation completed → settle → 201; every early exit releases the nonce; a failed `post` is 503 `escrow_post_failed` and never settles; a failed settle after `post` sets `tasks.float_absorbed` and still answers 201; a replay of a completed authorization answers 201 with `replay: true` and `buyer_token: null`. `POST /check` is the same screening with no chain, no gateway, no identity and no marker in its import graph. Caps: five open tasks from `tasks`, twenty-five dollars a day from `caps_ledger` keyed `(payer, UTC day)`.
- **Contract settled in this sync (`docs/api.md` regenerated):** `postTasks` lists 409 and 503; `conflict` carries `retry_after_s`; `escrow_post_failed` joins `GenericError`; `HEADERS.payerHint = 'X-Payer'`. **Still pending, on purpose:** the replayed 201 (`replay: true`, `buyer_token: null`) is not yet in `Posted` because T-28 is coding against `buyer_token: string` right now; the lead widens it once #T-28 merges.
- **`@types/ngeohash`** is in the catalog and both apps' devDependencies; the two `@ts-expect-error` blocks are gone.
- **The check route** computes its price with `priceWithFee` / `toUsdcUnits` from `@legwork/shared` — the same integers as `quoteFor` with no payments package in its imports (§14's letter).
- **Lessons:** a headless session that ends "waiting for CI" leaves the PR a draft — `gh pr ready` it after `gh pr checks`; the account's five-hour window killed T-16 twice, both resumed with `--resume`.
- **Still open for the lead:** the `Posted` widening above; the `tasks.resolved_to` column; the sessions schema columns; CI `graph test`; `secretOrAdminKey`; the observations reconcile behind `/admin/sweep`; structured `buyer_token_required`; `readJsonWithCap` wiring; T-33's `resolved` state; `<hours>`. Operator: T-04, then T-13/T-14; device passes; the rehearsal release.

