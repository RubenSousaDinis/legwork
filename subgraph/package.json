{
  "name": "@legwork/subgraph",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "typecheck": "echo 'subgraph typechecks via graph build'",
    "lint": "echo 'no lint for subgraph'",
    "test": "echo 'mapping tests run under matchstick: cd subgraph && pnpm graph test'"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "catalog:",
    "@graphprotocol/graph-ts": "catalog:",
    "assemblyscript": "catalog:",
    "matchstick-as": "catalog:"
  }
}
