{
  "name": "@legwork/examples",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo 'no tests'",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "catalog:",
    "@legwork/shared": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": { "typescript": "catalog:", "tsx": "catalog:", "@types/node": "catalog:" }
}
